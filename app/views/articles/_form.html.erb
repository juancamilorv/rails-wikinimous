<%= form_with model: article do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :content %>
  <%= f.text_field :content %>

  <% action = article.new_record? ? "CREATE" : "UPDATE" %>
  <%= f.submit action %>
<% end %>
